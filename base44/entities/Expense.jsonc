{
  "name": "Expense",
  "type": "object",
  "properties": {
    "trip_id": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "amount": {
      "type": "number"
    },
    "paid_by": {
      "type": "string",
      "description": "Email of who paid"
    },
    "paid_by_name": {
      "type": "string"
    },
    "split_among": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Emails of who shares the cost"
    },
    "custom_split_amounts": {
      "type": "object",
      "description": "Map of email -> custom amount for unequal splits"
    },
    "is_settled": {
      "type": "boolean",
      "default": false
    },
    "settlement_notes": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "enum": [
        "food",
        "transport",
        "lodging",
        "activity",
        "shopping",
        "other"
      ],
      "default": "other"
    },
    "trip_wide": {
      "type": "boolean",
      "default": true,
      "description": "If true, expense applies to the whole trip"
    },
    "day_number": {
      "type": "number",
      "description": "Trip day number (1-based) if not trip-wide"
    }
  },
  "required": [
    "trip_id",
    "description",
    "amount",
    "paid_by",
    "split_among"
  ]
}